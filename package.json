{
  "name": "angst",
  "private": true,
  "type": "module",
  "description": "Angst — planner semanal modular. modules/feed/ tiene su propio package.json (dependencias del pipeline de Actions, separadas de las del build principal).",
  "scripts": {
    "build": "node build/assemble.mjs",
    "test:architecture": "node tests/module-registry.mjs",
    "test:planner": "node tests/planner-domain.mjs && node tests/planner-state.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
